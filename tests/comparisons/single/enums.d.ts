export enum Strings {
  one = "one",
  two = "two",
}

export enum CamelCase {
  CamelCaseFirst = "camel_1",
  CamelCaseSecond = "camel_2",
}

export enum Ordinals {
  zero,
  one,
}

export enum Numbers {
  one = 1,
  two = 2,
  three,
}
